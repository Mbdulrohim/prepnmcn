{
  "name": "lambda-email-processor",
  "version": "1.0.0",
  "description": "AWS Lambda function to process incoming SES emails and forward to webhook",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "zip": "zip -r deploy/function.zip dist node_modules package.json package-lock.json",
    "start": "npm run build && npm run zip"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.156",
    "@types/mailparser": "^3.4.6",
    "@types/node": "^24.7.0",
    "@types/sanitize-html": "^2.16.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.901.0",
    "@aws-sdk/client-ses": "^3.901.0",
    "aws-lambda": "^1.0.7",
    "mailparser": "^3.7.4",
    "sanitize-html": "^2.17.0"
  }
}
